# SPA fallback — solo lo esencial.
# Cualquier URL que no sea un archivo o carpeta real sirve /index.html
# para que React Router maneje la ruta.

RewriteEngine On

RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

RewriteRule ^ /index.html [L]
