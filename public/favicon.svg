<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 64 64" fill="none">
  <!-- Background -->
  <rect width="64" height="64" rx="13" fill="#060d1f"/>

  <!-- Teal bottom accent bar -->
  <rect x="8" y="56" width="48" height="3" rx="1.5" fill="#00b4d8" opacity="0.6"/>

  <!-- C1 — left bar -->
  <rect x="7" y="17" width="4" height="28" rx="1.5" fill="#00b4d8"/>
  <!-- C1 — top bar -->
  <rect x="7" y="17" width="14" height="4" rx="1.5" fill="#00b4d8"/>
  <!-- C1 — bottom bar -->
  <rect x="7" y="41" width="14" height="4" rx="1.5" fill="#00b4d8"/>

  <!-- C2 — left bar -->
  <rect x="26" y="17" width="4" height="28" rx="1.5" fill="#00b4d8"/>
  <!-- C2 — top bar -->
  <rect x="26" y="17" width="14" height="4" rx="1.5" fill="#00b4d8"/>
  <!-- C2 — bottom bar -->
  <rect x="26" y="41" width="14" height="4" rx="1.5" fill="#00b4d8"/>

  <!-- G — left bar -->
  <rect x="45" y="17" width="4" height="28" rx="1.5" fill="#ffffff"/>
  <!-- G — top bar -->
  <rect x="45" y="17" width="14" height="4" rx="1.5" fill="#ffffff"/>
  <!-- G — bottom bar -->
  <rect x="45" y="41" width="14" height="4" rx="1.5" fill="#ffffff"/>
  <!-- G — right bar (bottom half only) -->
  <rect x="55" y="30" width="4" height="15" rx="1.5" fill="#ffffff"/>
  <!-- G — middle stub -->
  <rect x="49" y="30" width="10" height="4" rx="1.5" fill="#ffffff"/>
</svg>
